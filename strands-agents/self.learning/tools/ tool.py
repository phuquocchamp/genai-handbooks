from strands import tool

def weather_forecast(location: str, days: int) -> str:
    """
    Returns a weather forecast for the specified location and number of days.
    
    Args:
        location (str): The location for the weather forecast.
        days (int): The number of days to forecast.
        
    Returns:
        str: A formatted weather forecast.
    """
    # Placeholder implementation
    return f"Weather forecast for {location} for the next {days} days."